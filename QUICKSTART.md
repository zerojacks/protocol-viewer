# 快速开始指南

## 5 分钟上手 Protocol Viewer

### 第 1 步：确认环境

确保已安装：
- Rust 工具链（1.70+）
- Git

```bash
rustc --version
cargo --version
git --version
```

### 第 2 步：进入项目目录

```bash
cd /d/ProjackSpace/projectspace/protocol-viewer
```

### 第 3 步：构建项目

```bash
# 首次构建（较慢，需要下载 GPUI）
cargo build --release

# 或直接运行（自动构建）
cargo run --release
```

**注意**：首次构建可能需要 5-10 分钟，因为需要下载并编译 GPUI 框架。

### 第 4 步：启动应用

窗口打开后，你会看到：

```
┌─────────────────────────────────────────────────────┐
│ 报文: [68 49 00 40 04 11 ...]      [解析]          │
├─────────────────────────────────────────────────────┤
│  字段名            │  数据          │  说明          │
├─────────────────────────────────────────────────────┤
│ (解析结果将显示在这里)                               │
└─────────────────────────────────────────────────────┘
```

### 第 5 步：解析报文

#### 使用默认报文

应用已预填充一个 CSG 本地通信报文，直接点击"解析"按钮即可。

#### 使用自定义报文

1. 清空输入框
2. 粘贴你的十六进制报文（可以带空格）
3. 点击"解析"按钮

### 第 6 步：查看结果

解析成功后，树形结构会展示：

```
Q/CSG1209021-2019 解析结果
├── 链路层
│   ├── 起始符      │ 68H        │ 固定值
│   ├── 长度域      │ 0049H      │ 73字节
│   └── 控制字节    │ 40H        │ 下行主站
├── 应用层
│   ├── AFN        │ 04H        │ 数据转发
│   ├── SEQ        │ 17         │ 帧序列号
│   └── DI         │ E8020402H  │ 集中器
└── 数据域
    └── ...
```

### 第 7 步：交互操作

#### 展开/折叠节点

- 点击节点前的 ▼ 图标折叠
- 点击节点前的 ▶ 图标展开

#### 调整列宽

1. 鼠标移动到列之间的分隔条（竖线）
2. 光标变为 ↔️
3. 按住左键拖动

## 示例报文

### 示例 1：简单确认帧

```
68 0C 00 80 00 01 01 00 01 E8 6B 16
```

**说明**：
- 协议：CSG 本地通信
- AFN：00H（确认/否认）
- 长度：12 字节

### 示例 2：数据转发帧

```
68 49 00 40 04 11 02 04 02 E8 0A 18 39 36 00 19 00 50 39 36 00 19 00 07 09 37 00 19 00 85 31 39 00 19 00 35 24 45 00 20 00 48 24 45 00 20 00 27 52 46 00 20 00 24 56 46 00 20 00 18 58 46 00 20 00 26 77 46 00 20 00 56 16
```

**说明**：
- 协议：CSG 本地通信
- AFN：04H（数据转发）
- DI：E8020402H（集中器）
- 数据：60 字节

## 常见问题

### Q: 解析失败怎么办？

**检查清单**：
1. 报文是否完整（起始 68H 到结束 16H）
2. 十六进制字符是否正确（0-9, A-F）
3. 字符数是否为偶数
4. 校验和是否正确

### Q: 为什么有些协议不支持？

**当前状态**：
- ✅ Q/CSG1209021-2019（CSG 本地通信）- 完整支持
- ⚠️ DL/T 645-2007 - 暂不支持树形展示
- ⚠️ Q/CSG1209022-2019 - 暂不支持树形展示

### Q: 如何调整列宽？

1. 鼠标移动到列分隔条（竖线）
2. 光标变为 ↔️
3. 按住左键拖动

### Q: 应用运行缓慢？

**优化建议**：
- 使用 `--release` 模式运行
- 减少报文长度
- 关闭不必要的后台程序

## 键盘快捷键（待实现）

当前版本暂无快捷键支持，规划中的快捷键：

| 快捷键      | 功能             |
|-------------|------------------|
| Ctrl+V      | 粘贴报文         |
| Ctrl+Enter  | 解析报文         |
| Ctrl+E      | 展开所有节点     |
| Ctrl+C      | 折叠所有节点     |
| Ctrl+F      | 搜索             |
| Ctrl+S      | 保存结果         |

## 下一步

- 📖 阅读 [README.md](README.md) 了解详细功能
- 🔧 查看 [SUMMARY.md](SUMMARY.md) 了解技术实现
- 🐛 遇到问题？请提交 Issue

## 技术支持

- GitHub: [protocol-parser](https://github.com/your-repo/protocol-parser)
- 文档: [../protocol-parser/MULTI_PROTOCOL_PARSER.md](../protocol-parser/MULTI_PROTOCOL_PARSER.md)

---

**祝你使用愉快！** 🎉
