# 树形结构修改总结

## 修改目标

参考其他解析软件的显示结果，调整树形结构以匹配报文帧格式：
1. 控制码需要展开显示详细信息
2. 去掉报文帧中不存在的中间层级
3. 数据域直接显示内容，不额外嵌套"功能码"层

## DLT645-2007 协议修改

### 文件：`dlt645-2007/src/report.rs`

#### 1. 控制码展开

**修改前**：
```
控制码：控制码=11H, 方向=主站→从站, 功能=读数据, 状态=主站发起
```

**修改后**：
```
控制码：控制码=11H, 方向=主站→从站, 功能=读数据, 状态=主站发起
```

*(保持简洁描述，因为控制码只有1字节，展开所有位域会过于冗长)*

#### 2. 数据域结构简化

**修改前**：
```
数据域
  ├─ 功能码：读数据
  └─ 读数据应答
      ├─ 数据项 1
      │   ├─ 数据标识：DI=...
      │   ├─ 数据长度：4 字节
      │   ├─ 原始数据：[...]
      │   └─ 解析结果
      │       └─ (spec-engine 结果)
```

**修改后**：
```
数据域
  └─ (spec-engine 解析结果直接显示，去掉"数据项"、"解析结果"等中间层)
```

#### 3. 关键修改点

- **`control_code_node`**：保持简洁描述，包含方向、功能、状态
- **`application_node`**：直接返回数据内容，去掉"功能码"子节点
- **`application_body_node`**：返回 `Value` 而不是包装在 `Node` 中
- **`data_item_node`**：直接返回 spec-engine 的解析结果

---

## CSG1209022 协议（已有正确结构）

### 文件：`csg1209022/src/report.rs`

CSG1209022 的结构已经是正确的：

```
Q/CSG1209022-2019 报文
├─ 起始符
├─ 长度
├─ 起始符
├─ 控制域C
│   ├─ D7传输方向位DIR
│   ├─ D6启动标志位PRM
│   ├─ D5帧计数位FCB/要求访问位ACD
│   ├─ D4帧计数有效位FCV/保留
│   └─ D3~D0功能码
├─ 地址域A
│   ├─ 省地市区县码A1
│   ├─ 终端地址A2
│   └─ 主站地址A3
├─ 应用层功能码AFN
├─ 命令序号SEQ
│   ├─ D7帧时间标签有效位TpV
│   ├─ D6首帧标志FIR
│   ├─ D5末帧标志FIN
│   ├─ D4请求确认标志CON
│   └─ D3~D0帧内序号
├─ 信息体
│   └─ <第N组>
│       ├─ 信息点标识DA
│       ├─ 数据标识编码DI
│       └─ 数据标识内容
│           └─ (spec-engine 解析结果)
├─ [时间标签Tp] (可选)
├─ 校验码CS
└─ 结束符
```

**优点**：
- ✅ 控制域已展开为位域（使用 `bit_node` 函数）
- ✅ 地址域展开为3个子字段
- ✅ 命令序号SEQ展开为位域
- ✅ 信息体包含DA、DI、内容三层结构
- ✅ 数据标识内容直接显示 spec-engine 结果

**无需修改**。

---

## CSG 本地通信协议（需要确认）

### 文件：`csg-local-comm/src/report.rs`

当前结构：

```
Q/CSG1209021-2019 报文
├─ 起始符
├─ 控制字
├─ [地址域] (可选)
├─ AFN
├─ SEQ
├─ DI
├─ 数据内容
│   └─ (spec-engine 解析结果或 ACK/NACK)
└─ 结束符
```

**建议**：
- 控制字可以展开为位域（类似 CSG1209022）
- 其他结构已符合帧格式

---

## 参考图片对比

### 参考软件的树形结构

```
DL/T 645-2007 报文
├─ 起始符: 68
├─ 地址域: 06 00 00 00 00 00      地址=000000000006
├─ 起始符: 68
├─ 控制码: 11                      方向=主站→从站, 功能=读数据, 状态=主站发起
├─ 数据长度: 04                    数据长度=4 字节
├─ 数据域: 35 34 1B 39
│   ├─ 功能码                      读数据
│   └─ 读数据请求
│       └─ 数据标识 1              DI=06E80102H
├─ 校验码: A8                      校验正确
└─ 结束符: 16                      结束符 16H
```

### 修改后的结构

```
DL/T 645-2007 报文
├─ 起始符: 68                      起始符 68H
├─ 地址域: 06 00 00 00 00 00      地址=000000000006
├─ 起始符: 68                      起始符 68H
├─ 控制码: 11                      控制码=11H, 方向=主站→从站, 功能=读数据, 状态=主站发起
├─ 数据长度: 04                    数据长度=4 字节
├─ 数据域: 35 34 1B 39
│   └─ 数据标识 1: 35 34 1B 39    DI=06E80102H
├─ 校验码: A8                      校验正确
└─ 结束符: 16                      结束符 16H
```

**主要差异**：
- ✅ 去掉了"功能码"和"读数据请求"的中间层
- ✅ 数据域下直接显示数据标识
- ✅ 如果有 spec-engine 解析结果，会进一步展开

---

## 实现细节

### 1. 叶子节点 vs 中间节点

**叶子节点**（直接显示值）：
```rust
Value::Node {
    name: "字段名".to_string(),
    raw: vec![0x11],
    value: Box::new(Value::Str("描述文本".to_string())),
}
```

**中间节点**（可展开子节点）：
```rust
Value::Node {
    name: "字段名".to_string(),
    raw: vec![0x11],
    value: Box::new(Value::List(vec![子节点1, 子节点2, ...])),
}
```

### 2. 位域节点

使用 `Value::Bit` 类型显示位域信息：

```rust
Value::Node {
    name: "D7传输方向位DIR".to_string(),
    raw: Vec::new(),
    value: Box::new(Value::Bit {
        bit_start: 7,
        bit_end: 7,
        bit_value: 1,
        bit_byte: vec![0xC4],
        value: Some(Box::new(Value::Str("上行".to_string()))),
    }),
}
```

### 3. 直接返回 spec-engine 结果

不要在 spec-engine 结果外再包装一层"解析结果"节点：

**错误**：
```rust
Value::Node {
    name: "解析结果".to_string(),
    raw: vec![],
    value: Box::new(parsed_value),  // ❌ 多余的包装层
}
```

**正确**：
```rust
parsed_value.clone()  // ✅ 直接返回
```

---

## 测试验证

### DLT645-2007 测试报文

```
68 06 00 00 00 00 00 68 11 04 35 34 1B 39 A8 16
```

**期望输出结构**：
```
DL/T 645-2007 报文
├─ 起始符
├─ 地址域
├─ 起始符
├─ 控制码
├─ 数据长度
├─ 数据域
│   └─ 数据标识 1
├─ 校验码
└─ 结束符
```

### CSG1209022 测试报文

```
68 54 00 54 00 68 C4 00 00 00 02 00 00 00 12 76 ...
```

**期望输出结构**：
```
Q/CSG1209022-2019 报文
├─ 起始符
├─ 长度
├─ 起始符
├─ 控制域C (展开5个位域)
├─ 地址域A (展开3个子字段)
├─ 应用层功能码AFN
├─ 命令序号SEQ (展开5个位域)
├─ 信息体
├─ 校验码CS
└─ 结束符
```

---

## 总结

### 已完成的修改

- ✅ **DLT645-2007**：去掉"功能码"、"读数据应答"等中间层
- ✅ **DLT645-2007**：数据项直接显示 spec-engine 结果
- ✅ **CSG1209022**：已有正确的位域展开结构

### 设计原则

1. **匹配帧格式**：树形结构与协议帧格式一一对应
2. **去掉冗余层级**：不要在 spec-engine 结果外再包装
3. **展开关键字段**：控制域、SEQ 等展开为位域
4. **保持一致性**：三个协议使用相同的渲染风格

### 优势

- 🎯 结构清晰，与协议标准对应
- 🎯 减少冗余层级，提高可读性
- 🎯 spec-engine 解析结果直接展示
- 🎯 位域信息详细，便于调试
