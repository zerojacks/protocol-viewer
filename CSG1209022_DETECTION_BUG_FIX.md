# CSG1209022 协议检测 Bug 修复

## 问题描述

用户提供的合法 CSG1209022 报文无法被 `protocol-parser` 识别，返回 "无法识别协议类型" 错误。

**问题报文**：
```
68 54 00 54 00 68 C4 00 00 00 02 00 00 00 12 76 00 00 02 03 00 E0 00 01 01 20 01 01 00 E8 05 00 32 00 00 F4 24 06 00 0F 00 00 13 88 05 00 19 00 00 92 7C 04 00 3C 00 00 27 10 03 00 1E 00 00 61 A8 02 00 2D 00 01 86 A0 01 30 15 28 07 25 2F 26 07 29 00 00 20 26 07 29 00 00 F6 16
```

## 根本原因分析

### Bug 1: 长度域检查错误

**位置**：`csg1209022/src/lib.rs` 的 `is_csg1209022_frame` 函数

**错误代码**：
```rust
let frame_len = u16::from_le_bytes([buf[1], buf[2]]) as usize;
let frame_len2 = u16::from_le_bytes([buf[1], buf[2]]) as usize;  // ❌ 错误：重复读取同样的字节
```

**问题**：
- CSG1209022 协议的长度域出现**两次**：`[1-2]` 和 `[3-4]`
- 但代码重复读取了 `[1-2]`，没有读取 `[3-4]`
- 导致无法真正验证两次长度是否一致

**正确代码**：
```rust
let frame_len = u16::from_le_bytes([buf[1], buf[2]]) as usize;
let frame_len2 = u16::from_le_bytes([buf[3], buf[4]]) as usize;  // ✅ 正确：读取第二次出现的长度
```

---

### Bug 2: 结束符位置错误

**错误代码**：
```rust
// 检查结束符
if buf[frame_len - 1] != 0x16 {  // ❌ 错误：frame_len 是用户数据长度，不是总长度
    return false;
}
```

**问题**：
- `frame_len` 是用户数据区（控制域+地址域+应用层）的长度
- 结束符在整个帧的最后，应该用 `buf.len() - 1`

**示例**：
```
frame_len = 84（用户数据区）
总长度 = 84 + 8 = 92
结束符位置 = buf[91]（即 buf.len() - 1）
但错误代码检查的是 buf[83]（frame_len - 1）
```

**正确代码**：
```rust
// 检查结束符
if buf[buf.len() - 1] != 0x16 {  // ✅ 正确：检查整个缓冲区的最后一个字节
    return false;
}
```

---

### Bug 3: 缺少第二个起始符检查

**错误代码**：
```rust
// 检查起始符
if buf[0] != 0x68 {
    return false;
}
```

**问题**：
- 只检查了第一个起始符 `[0]`
- 没有检查第二个起始符 `[5]`
- 导致无法区分 CSG1209022 和其他协议

**正确代码**：
```rust
// 检查起始符（两个 68H）
if buf[0] != 0x68 || buf[5] != 0x68 {
    return false;
}
```

---

## 修复方案

### 完整的修复代码

**文件**：`protocol-parser/csg1209022/src/lib.rs`

```rust
/// 检测给定的字节数组是否为 Q/CSG1209022-2019 协议帧
///
/// 检测规则：
/// 1. 起始符必须是 68H
/// 2. 长度域必须合理且两次出现一致
/// 3. 第二个起始符必须是 68H
/// 4. 结束符必须在正确的位置且为 16H
pub fn is_csg1209022_frame(buf: &[u8]) -> bool {
    use link::frame::Frame;
    
    // 最小长度：68H + L(2) + L(2) + 68H + C(1) + A(7) + CS(1) + 16H = 22字节
    if buf.len() < 22 {
        return false;
    }

    // 检查起始符（两个 68H）
    if buf[0] != 0x68 || buf[5] != 0x68 {
        return false;
    }

    // 检查长度域（两次出现必须一致）
    let frame_len = u16::from_le_bytes([buf[1], buf[2]]) as usize;
    let frame_len2 = u16::from_le_bytes([buf[3], buf[4]]) as usize;
    if frame_len != frame_len2 {
        return false;
    }

    // 检查长度合理性
    if frame_len < 7 || frame_len > 65535 {
        return false;
    }

    // 检查总长度：L + 固定头尾(8字节) = 实际长度
    if (frame_len + 8) != buf.len() {
        return false;
    }

    // 检查结束符
    if buf[buf.len() - 1] != 0x16 {
        return false;
    }

    // 尝试完整解析以验证是否为有效的 CSG1209022 帧
    Frame::decode(buf).is_ok()
}
```

---

## 验证结果

### 问题报文验证

使用修复后的代码，问题报文现在可以正确识别：

```
报文: 68 54 00 54 00 68 C4 00 00 00 02 00 00 00 12 76 ...
  [0] = 0x68 ✓ (起始符)
  [1-2] = 84 (长度L1)
  [3-4] = 84 (长度L2) ✓
  [5] = 0x68 ✓ (第二个起始符)
  总长度 = 84 + 8 = 92 ✓
  [91] = 0x16 ✓ (结束符)
  
  => 成功识别为 Q/CSG1209022-2019 协议
```

---

## 协议检测优先级

当前 `protocol-parser` 的协议检测优先级：

1. **CSG 本地通信** (Q/CSG1209021-2019)
2. **DLT645-2007**
3. **CSG1209022** (Q/CSG1209022-2019)

这个优先级是合理的，因为：
- CSG 本地通信最特殊（单 68H 起始符）
- DLT645 和 CSG1209022 都是双 68H，但有明确的区分特征
- DLT645 先检测不会误判 CSG1209022（第二个 68H 位置不同）

---

## 协议特征对比

| 特征 | DLT645-2007 | CSG1209022 | CSG 本地通信 |
|------|-------------|------------|--------------|
| 起始符 | 68H | 68H | 68H |
| 第二个 68H | [7] | [5] | 无 |
| 长度域 | [9] 单字节 | [1-4] 2字节重复 | [1-2] 2字节 |
| 地址域 | [1-6] 6字节BCD | [6-13] 7字节 | [3-14] 12字节 |
| 数据偏移 | +33H | 无 | 无 |
| 结束符 | 16H | 16H | 16H |

---

## 测试建议

建议添加测试用例：

```rust
#[test]
fn test_is_csg1209022_frame_real_data() {
    let frame = vec![
        0x68, 0x54, 0x00, 0x54, 0x00, 0x68, 0xC4, 0x00, 
        0x00, 0x00, 0x02, 0x00, 0x00, 0x00, 0x12, 0x76,
        // ... 完整报文
    ];
    assert!(is_csg1209022_frame(&frame));
}

#[test]
fn test_is_csg1209022_frame_not_dlt645() {
    // DLT645 帧不应被误判为 CSG1209022
    let frame = vec![
        0x68, 0x12, 0x90, 0x78, 0x56, 0x34, 0x12, 0x68,
        0x11, 0x04, 0x33, 0x33, 0x34, 0x33, 0xEB, 0x16,
    ];
    assert!(!is_csg1209022_frame(&frame));
}
```

---

## 总结

修复了三个关键 bug：
1. ✅ 长度域读取错误（读取了两次相同位置）
2. ✅ 结束符位置计算错误
3. ✅ 缺少第二个起始符检查

现在 CSG1209022 协议可以正确识别了！
